# TECHNICAL REFERENCE DOCUMENT
## AboutWallArt SEO Tools Infrastructure

**Last Updated:** May 29, 2026 (GSC API Setup + OAuth Credentials + GSC Report Tool in progress)
**Owner:** aboutwallart
**Purpose:** Complete technical documentation for all SEO tools, infrastructure, API keys, and configurations

---

# GITHUB REPOSITORY

## Repository Information
- **Repository:** aboutwallart/seo-tools
- **URL:** https://github.com/aboutwallart/seo-tools
- **Visibility:** Public
- **Main Branch:** main
- **Languages:** HTML (84%), JavaScript (16%)

## Repository Structure

### Root Level Files
- `index.html` — Password-protected landing page with tools dashboard
- `auth-check.js` — Authentication check script (protects all tools)
- `blog-manager-tool.html` — Blog content management tool (password protected)
- `keyword-locker.html` — Keyword locking and conflict detection tool (password protected)
- `money-page-doctor.html` — Money page optimization tool (password protected)
- `rank-report-card.html` — Ranking report and tracking tool (password protected)
- `image-optimizer.html` — Shopify image optimization tool (password protected)
- `gsc-report.html` — GSC SEO Report dashboard (IN PROGRESS - May 29, 2026)
- `package.json` — Node.js dependencies
- `vercel.json` — Vercel deployment configuration with domain redirects

---

# FOLDER STRUCTURE

## `/api/` Folder
**Purpose:** Serverless API endpoints (Vercel Functions)

### ⚠️ VERCEL FUNCTION STATUS (Updated May 29, 2026)
**Vercel Hobby Plan allows maximum 12 serverless functions.**
**The project was AT the limit (12 functions) but now has 1 free slot available.**

**Why a slot is available:**
- `reddit.js` — No longer in use
- `openai.js` — No longer in use
- One slot was used to create `gsc.js` (May 29, 2026)
- Current count: 11/12 (1 slot remaining: either reddit.js or openai.js can still be replaced)

**DO NOT create new API files beyond the remaining slot.** If a new tool needs a backend operation, add it as a new action inside an existing API file.

### Current API Files (11/12 after gsc.js added):
1. **`auth.js`** — Password authentication endpoint
2. **`analyze-money-page.js`** — Money Page Optimizer backend
3. **`blogs.js`** — Blog Manager backend (see full details below)
4. **`claude.js`** — Claude (Anthropic) API integration
5. **`fetch-html.js`** — Fetches HTML content from URLs
6. **`generate-alt-text.js`** — Alt text generation
7. **`migrate.js`** — Data migration scripts
8. **`openai.js`** — OpenAI API integration (NO LONGER IN USE)
9. **`pagespeed.js`** — Google PageSpeed Insights API
10. **`reddit.js`** — Reddit scraper for blog topic ideas (NO LONGER IN USE)
11. **`serpapi.js`** — SERP API integration
12. **`shopify-files.js`** — Shopify Admin GraphQL API integration
13. **`gsc.js`** — Google Search Console API integration (NEW - May 29, 2026) *(see full details below)*

---

## gsc.js — Full Technical Details

**File:** `api/gsc.js`
**Added:** May 29, 2026
**Purpose:** GSC Report tool backend — connects to Google Search Console API using OAuth 2.0

### Authentication Flow:
- Uses OAuth 2.0 with offline access (refresh token — never expires)
- Credentials stored as Vercel env vars (see API Keys section)
- No user login required — uses pre-authorized refresh token for mae@aboutwallart.com
- GSC property: `https://www.aboutwallart.com/`

### Google Cloud Project:
- **Project:** Money Page Optimizer (reused existing project)
- **Project ID:** `money-page-optimizer`
- **Project Number:** `15466013888`
- **GSC API:** Enabled (Google Search Console API)

### OAuth App Details:
- **App Name:** About Wall Art GSO Tools
- **OAuth Client Name:** About Wall Art GSO Tools
- **Client Type:** Web application
- **Authorized JavaScript Origins:** `https://tools.aboutwallart.com`
- **Authorized Redirect URIs:** `https://tools.aboutwallart.com/api/gsc`
- **User Type:** External (testing mode)
- **Test User:** mae@aboutwallart.com

### How to get a new Refresh Token (if needed):
1. Go to this URL in browser:
```
https://accounts.google.com/o/oauth2/auth?client_id=15466013888-deual871humreqd3uqhrve8chp3bci3s.apps.googleusercontent.com&redirect_uri=https://tools.aboutwallart.com/api/gsc&response_type=code&scope=https://www.googleapis.com/auth/webmasters.readonly&access_type=offline&prompt=consent
```
2. Sign in as mae@aboutwallart.com
3. Allow access
4. Copy the `code` parameter from the redirect URL
5. Go to hoppscotch.io → POST → `https://oauth2.googleapis.com/token`
6. Body: application/x-www-form-urlencoded with these 5 params:
   - `code` = the code from step 4
   - `client_id` = `15466013888-deual871humreqd3uqhrve8chp3bci3s.apps.googleusercontent.com`
   - `client_secret` = `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`
   - `redirect_uri` = `https://tools.aboutwallart.com/api/gsc`
   - `grant_type` = `authorization_code`
7. Send — copy `refresh_token` from response
8. Update `GSC_REFRESH_TOKEN` in Vercel env vars
9. Redeploy Vercel

---

## blogs.js — Full Technical Details

**File:** `api/blogs.js`
**Last Updated:** May 27, 2026
**Purpose:** Blog Manager tool backend — handles all blog data operations AND Google Sheets integration

### Supported HTTP Methods:

#### GET — Read published blogs from registry
- Reads `data/keyword-locker-registry.csv` from local filesystem
- Filters rows where `Source` column (index 9) = `Published Blog`
- Returns: `{ blogs: [{title, keyword, status, url, date}] }`

#### POST — Mark blog as "To Write"
- Reads `keyword`, `url`, `title`, `perspective` from request body
- Does 3 things in sequence:
  1. Appends row to `data/keyword-locker-registry.csv` on GitHub (via GitHub API)
  2. Updates `data/blog_ideas.csv` on GitHub — sets STATUS column to `TO_WRITE`
  3. Calls Google Sheets webhook (non-blocking — won't fail if Sheets fails)
- Returns: `{ success: true, keyword, url, sheetsResult }`

#### PATCH — Mark as Keyword Change
- Updates `data/blog_ideas.csv` — sets STATUS to `KEYWORD_CHANGE`

#### DELETE — Undo
- Removes entry from `data/keyword-locker-registry.csv`
- Clears STATUS in `data/blog_ideas.csv`

### Google Sheets Integration (added May 27, 2026):
- Uses Google Apps Script Web App as webhook (no OAuth/credentials needed)
- Apps Script runs as the Google account owner, has automatic Sheet access
- Webhook URL stored in Vercel env var: `SHEETS_WEBHOOK_URL`
- If `SHEETS_WEBHOOK_URL` not set, Sheets append is silently skipped
- Non-blocking: if Sheets fails, the whole request still succeeds
- Column mapping when row is appended:
  - Column A (index 0) = `BLOG MANAGER TOOL` (fixed text)
  - Column B (index 1) = Perspective
  - Column C (index 2) = Main Keyword
  - Column F (index 5) = Blog Post Title
  - Column AS (index 44) = `READY TO GENERATE BLOG` (fixed text)
  - All other columns = empty

### Google Apps Script (Sheets Webhook):
- **Script name:** Sheets Webhook
- **Google Sheet ID:** `1W9AYH2Zcw-v2U6S47a3w5bs5rEBf6K30adDgNkaZ38k`
- **Tab:** Sheet1
- **Deployed as:** Web App, Execute as: Me, Who has access: Anyone
- **Script code:**
```javascript
function doPost(e) {
  const sheet = SpreadsheetApp.openById('1W9AYH2Zcw-v2U6S47a3w5bs5rEBf6K30adDgNkaZ38k').getSheetByName('Sheet1');
  const data = JSON.parse(e.postData.contents);
  const row = new Array(45).fill('');
  row[0]  = data.colA;
  row[1]  = data.colB;
  row[2]  = data.colC;
  row[5]  = data.colF;
  row[44] = data.colAS;
  sheet.appendRow(row);
  return ContentService.createTextOutput(JSON.stringify({ success: true }))
    .setMimeType(ContentService.MimeType.JSON);
}
```

### CORS Headers (ALL api files must include):
```javascript
res.setHeader('Access-Control-Allow-Credentials', true);
res.setHeader('Access-Control-Allow-Origin', '*');
res.setHeader('Access-Control-Allow-Methods', 'GET,POST,DELETE,PATCH,OPTIONS');
res.setHeader('Access-Control-Allow-Headers', 'X-CSRF-Token, X-Requested-With, Accept, Accept-Version, Content-Length, Content-MD5, Content-Type, Date, X-Api-Version');
if (req.method === 'OPTIONS') { res.status(200).end(); return; }
```

---

## claude.js — Full Technical Details

**CRITICAL: Model name must be exactly `claude-sonnet-4-6`**
Other model names will fail. Do not use `claude-3`, `claude-sonnet`, or any other variant.

### Web Search Tool:
To enable web search in Claude API calls, add to the API request:
```javascript
tools: [
  {
    type: "web_search_20250305",
    name: "web_search"
  }
]
```

### Standard API Call:
```javascript
const response = await fetch('https://api.anthropic.com/v1/messages', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'x-api-key': process.env.ANTHROPIC_API_KEY,
    'anthropic-version': '2023-06-01'
  },
  body: JSON.stringify({
    model: 'claude-sonnet-4-6',  // MUST be exactly this
    max_tokens: 4096,
    messages: [{ role: 'user', content: prompt }],
    tools: [{ type: 'web_search_20250305', name: 'web_search' }]  // if needed
  })
});
```

---

## `/data/` Folder

### Active Data Files:

#### `keyword-locker-registry.csv`
- Central database of all locked keywords
- Prevents keyword cannibalization
- All tools read from and write to this file
- Column structure: `Keyword, Page URL, LOCKED, Status, Action, Clicks, Position, Match Score, AI Score, Source`
- Source values: `Published Blog`, `To_Write_Blog`, `Collection Page`, etc.
- 378+ published blogs as of May 27, 2026

#### `blog_ideas.csv` (NEW - May 27, 2026)
- Blog idea pipeline — list of candidate blog posts with conflict detection
- Column structure: `Keyword, Blog Post Title, Perspective, CANNIBALIZATION, CONFLICTING KEYWORD, CONFLICTING BLOG URL, STATUS`
- CANNIBALIZATION values: `NO CONFLICT`, `CAUTION`, `DANGER`
- STATUS values: `TO_WRITE`, `TO_WRITE` (from registry), `KEYWORD_CHANGE`, empty
- Read by `blogs.js` GET handler
- Updated by `blogs.js` POST/PATCH/DELETE handlers
- Currently contains 50 processed ideas

### Blog Ideas Conflict Detection Algorithm:
```python
# Jaccard similarity on core words
stop_words = {'how','to','a','an','the','your','my','for','in','with',...}
def extract_core(keyword): return [w for w in keyword.lower().split() if w not in stop_words and len(w) > 2]
def jaccard(k1, k2):
    w1, w2 = set(extract_core(k1)), set(extract_core(k2))
    return len(w1 & w2) / len(w1 | w2)

# Thresholds:
# >= 0.6 → DANGER
# >= 0.4 → CAUTION
# < 0.4  → NO CONFLICT
```

### STATUS sync rule:
When processing new blog ideas, check `keyword-locker-registry.csv` for rows where Source = `To_Write_Blog`. If keyword matches, set STATUS = `TO_WRITE` in blog_ideas.csv.

---

# BLOG MANAGER TOOL

## Overview
**File:** `blog-manager-tool.html`
**Current Version:** v1.9 (May 27, 2026)
**URL:** https://tools.aboutwallart.com/blog-manager-tool.html

## Features
- 3 tabs: Blog Ideas | To Write | Keyword Change
- Reads `blog_ideas.csv` from GitHub via `/api/blogs` GET
- Conflict detection shown per card: NO CONFLICT (green), CAUTION (yellow), DANGER (red)
- "Mark for Writing" button: saves to registry + marks blog_ideas.csv + sends to Google Sheet
- "Send to Google Sheet" button (manual, in To Write tab): sends to Sheet and removes card from view
- "Undo" button: removes from registry, clears status
- "Change Keyword" button: marks for keyword review

## Version History:
- v1.7 — 3 tabs working, Mark for Writing saves to registry and blog_ideas.csv
- v1.8 — Added `title` and `perspective` to POST body for Google Sheets
- v1.9 — Added "Send to Google Sheet" button in To Write tab (manual trigger, card disappears on send)

## Mark for Writing — POST body sent to /api/blogs:
```javascript
{
  keyword: idea.currentKeyword,
  url: generateUrl(idea.currentTitle),
  title: idea.currentTitle,
  perspective: idea.perspective
}
```

---

# GSC REPORT TOOL (IN PROGRESS - May 29, 2026)

## Overview
**File:** `gsc-report.html` (to be created)
**API:** `api/gsc.js` (to be created)
**Status:** IN PROGRESS
**Goal:** Replicate HeyTony SEO Report dashboard using live GSC data

## Planned Sections:
1. High Level SEO Metrics (clicks, impressions, CTR, avg position)
2. Historical View - 1 Year (monthly trends)
3. LLM Traffic tracking (ChatGPT, Perplexity referrals)
4. Blog Tracking (track performance of specific blog posts)
5. Content Optimization Ideas (WHY/WHAT/HOW queries with no clicks)

---

# API KEYS & CREDENTIALS

## Vercel Environment Variables (current as of May 29, 2026)

```
ANTHROPIC_API_KEY        — Claude API key (Production only)
OPENAI_API_KEY           — OpenAI API key
SHOPIFY_STORE_DOMAIN     — aboutwallart.myshopify.com
SHOPIFY_ACCESS_TOKEN     — Shopify Admin API token
TOOLS_PASSWORD           — Password for tools dashboard authentication
GOOGLE_API_KEY           — Google API key (PageSpeed)
SERPAPI_KEY              — SerpAPI key (Production only)
BLOGDATA_READ_WRITE_TOKEN — Vercel Blob storage token
EDGE_CONFIG              — Edge config (all environments)
GITHUB_TOKEN             — GitHub API token for reading/writing data files
SHEETS_WEBHOOK_URL       — Google Apps Script Web App URL for Sheets integration (added May 27, 2026)
GSC_CLIENT_ID            — Google OAuth Client ID for GSC API (added May 29, 2026)
GSC_CLIENT_SECRET        — Google OAuth Client Secret for GSC API (added May 29, 2026)
GSC_REFRESH_TOKEN        — Google OAuth Refresh Token for GSC API (added May 29, 2026)
```

### How to Access:
1. Go to: https://vercel.com/aboutwallart/seo-tools
2. Settings → Environment Variables

### After adding/changing env vars:
**Must redeploy Vercel for changes to take effect.**
Go to Vercel → Deployments → 3 dots on latest → Redeploy

---

# CORS CONFIGURATION

All API files MUST include CORS headers at the top of the handler. See `blogs.js` section above for the standard headers block. This is required to prevent browser CORS errors when the frontend calls the API.

**vercel.json** also sets CORS headers at infrastructure level:
```json
{
  "redirects": [{
    "source": "/:path*",
    "has": [{ "type": "host", "value": "seo-tools-lake.vercel.app" }],
    "destination": "https://tools.aboutwallart.com/:path*",
    "permanent": true
  }],
  "headers": [{
    "source": "/api/(.*)",
    "headers": [
      { "key": "Access-Control-Allow-Origin", "value": "*" },
      { "key": "Access-Control-Allow-Methods", "value": "GET, POST, PUT, DELETE, OPTIONS" },
      { "key": "Access-Control-Allow-Headers", "value": "Content-Type, Authorization" }
    ]
  }]
}
```

---

# DEPLOYMENT

## GitHub → Vercel Workflow
1. Upload/edit file on GitHub (main branch)
2. Vercel auto-deploys (takes 2-3 minutes)
3. **Wait 2 minutes before testing**
4. Hard refresh browser: Cmd+Shift+R (Mac) or Ctrl+Shift+R (Windows)

## Debugging
- Vercel logs: https://vercel.com/aboutwallarts-projects/seo-tools/logs
- Check latest error timestamp
- Common issues:
  - Wrong Claude model name (must be `claude-sonnet-4-6`)
  - Missing environment variable
  - Vercel hasn't redeployed yet
  - Browser cache (hard refresh)

---

# DEPLOYED TOOLS

## Live URLs
**Primary Domain:** https://tools.aboutwallart.com (Password Protected)
**Old Domain:** https://seo-tools-lake.vercel.app (Redirects to tools.aboutwallart.com)

### Available Tools:
1. **Keyword Locker** — `/keyword-locker.html`
2. **Blog Manager** — `/blog-manager-tool.html`
3. **Money Page Doctor** — `/money-page-doctor.html`
4. **Rank Report Card** — `/rank-report-card.html`
5. **Image Optimizer** — `/image-optimizer.html`
6. **GSC Report** — `/gsc-report.html` *(IN PROGRESS)*

---

# PASSWORD PROTECTION SYSTEM

All tools require authentication. Each tool HTML file includes:
```html
<body>
    <script src="/auth-check.js"></script>
```

- Token stored in localStorage, 90-day expiry
- Password stored in Vercel env var: `TOOLS_PASSWORD`
- Login page: `index.html`
- Auth endpoint: `/api/auth.js`

---

# IMAGE OPTIMIZER TOOL

**File:** `image-optimizer.html`
**API:** `api/shopify-files.js`
**Status:** Production

## Key Technical Details:
- `fileUpdate` mutation is BROKEN in Shopify API — silently fails, does not persist alt text
- Use `replace_file` action for ALL operations (creates new file, deletes old)
- 3-second delay required between file create and delete
- Theme section images cannot be updated via API — must be done manually in theme editor

## Shopify API:
- Endpoint: `https://aboutwallart.myshopify.com/admin/api/2025-01/graphql.json`
- Env vars: `SHOPIFY_STORE_DOMAIN`, `SHOPIFY_ACCESS_TOKEN`

---

# BLOG IDEAS PROCESSING WORKFLOW

## How to add more ideas to blog_ideas.csv:

1. User provides a source file (xlsx or csv) with new blog ideas
2. Download current `blog_ideas.csv` from GitHub — note which keywords are already in it
3. Filter out rows already in blog_ideas.csv
4. Take next N rows (recommend batches of 10-50)
5. Run Jaccard conflict detection against `keyword-locker-registry.csv`
6. For each new row: check if keyword exists in registry with Source=`To_Write_Blog` → if yes, set STATUS=`TO_WRITE`
7. Append new rows to existing blog_ideas.csv (DO NOT replace — preserves previous work)
8. Upload updated blog_ideas.csv to GitHub at `data/blog_ideas.csv`

## Current state (May 27, 2026):
- blog_ideas.csv: 50 rows processed

---

# TROUBLESHOOTING

## CORS Errors
- Verify CORS headers are in the API file
- Verify `vercel.json` has correct CORS config
- Redeploy after changes
- Hard refresh browser

## Claude API fails
- Check model name: must be exactly `claude-sonnet-4-6`
- Check `ANTHROPIC_API_KEY` is set in Vercel env vars
- Check Vercel logs for specific error

## Google Sheets not receiving data
- Check `SHEETS_WEBHOOK_URL` is set in Vercel env vars
- Verify Apps Script is deployed as Web App with "Anyone" access
- Check Vercel logs — `sheetsResult` in API response shows success/error
- Non-fatal: even if Sheets fails, the rest of the save still works

## blog_ideas.csv not loading in tool
- Check file exists at `data/blog_ideas.csv` in GitHub
- Check `blogs.js` GET handler — reads from local filesystem, file must be in `data/` folder at repo root
- Verify Vercel has redeployed after CSV update

## Vercel function limit
- Was at 12/12, now at 11/12 after adding gsc.js (reddit.js and openai.js are unused)
- Do not add more than 1 additional API file without removing unused ones first

## GSC API fails
- Check `GSC_CLIENT_ID`, `GSC_CLIENT_SECRET`, `GSC_REFRESH_TOKEN` are set in Vercel env vars
- Refresh token may have expired — follow "How to get a new Refresh Token" steps in gsc.js section
- Check Vercel logs for specific error
- Verify Vercel has redeployed after env var changes

---

# CONTACT & SUPPORT

**GitHub:** https://github.com/aboutwallart/seo-tools
**Vercel:** https://vercel.com/aboutwallart/seo-tools
**Live Tools:** https://tools.aboutwallart.com
**Shopify Admin:** https://aboutwallart.myshopify.com/admin
**Google Sheet (Blog Pipeline):** https://docs.google.com/spreadsheets/d/1W9AYH2Zcw-v2U6S47a3w5bs5rEBf6K30adDgNkaZ38k
**Google Cloud Console:** https://console.cloud.google.com (project: money-page-optimizer)

---

**Document End**

This document should be updated whenever:
- New tools are added
- API keys are changed
- New env vars are added to Vercel
- CORS configuration is modified
- File structure changes
- New features added to existing tools
- Vercel function count changes
