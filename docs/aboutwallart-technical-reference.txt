# TECHNICAL REFERENCE DOCUMENT
## AboutWallArt SEO Tools Infrastructure

**Last Updated:** May 30, 2026 (Easy SEO Report Tool v52 — full build complete)
**Owner:** aboutwallart
**Purpose:** Complete technical documentation for all SEO tools, infrastructure, API keys, and configurations

---

# GITHUB REPOSITORY

## Repository Information
- **Repository:** aboutwallart/seo-tools
- **URL:** https://github.com/aboutwallart/seo-tools
- **Visibility:** Public
- **Main Branch:** main
- **Languages:** HTML (84%), JavaScript (16%)

## Repository Structure

### Root Level Files
- `index.html` — Password-protected landing page with tools dashboard
- `auth-check.js` — Authentication check script (protects all tools)
- `blog-manager-tool.html` — Blog content management tool (password protected)
- `keyword-locker.html` — Keyword locking and conflict detection tool (password protected)
- `money-page-doctor.html` — Money page optimization tool (password protected)
- `rank-report-card.html` — Ranking report and tracking tool (password protected)
- `image-optimizer.html` — Shopify image optimization tool (password protected)
- `gsc-report.html` — Easy SEO Report Tool (LIVE - v52 as of May 30, 2026)
- `package.json` — Node.js dependencies
- `vercel.json` — Vercel deployment configuration with domain redirects

---

# FOLDER STRUCTURE

## `/api/` Folder
**Purpose:** Serverless API endpoints (Vercel Functions)

### VERCEL FUNCTION STATUS (Updated May 30, 2026)
**Vercel Hobby Plan allows maximum 12 serverless functions.**
**Current count: 11/12 (1 slot remaining)**
- `reddit.js` — No longer in use (can be replaced)
- `openai.js` — No longer in use (can be replaced)
**DO NOT create new API files beyond the remaining slot.**

### Current API Files (11/12):
1. `auth.js` — Password authentication endpoint
2. `analyze-money-page.js` — Money Page Optimizer backend
3. `blogs.js` — Blog Manager + GSC Report backend (see full details below)
4. `claude.js` — Claude (Anthropic) API integration
5. `fetch-html.js` — Fetches HTML content from URLs
6. `generate-alt-text.js` — Alt text generation
7. `migrate.js` — Data migration scripts
8. `openai.js` — OpenAI API integration (NO LONGER IN USE)
9. `pagespeed.js` — Google PageSpeed Insights API
10. `reddit.js` — Reddit scraper (NO LONGER IN USE)
11. `serpapi.js` — SERP API integration
12. `shopify-files.js` — Shopify Admin GraphQL API + get-blogs-by-month action
13. `gsc.js` — Google Search Console API + GA4 (see full details below)

---

## gsc.js — Full Technical Details

**File:** `api/gsc.js`
**Last Updated:** May 30, 2026
**Purpose:** Easy SEO Report backend — GSC + GA4 data

### Authentication:
- OAuth 2.0 with offline access (refresh token)
- No user login required — uses pre-authorized refresh token for mae@aboutwallart.com
- GSC property: `sc-domain:aboutwallart.com` (domain property — NOT URL prefix)
- GA4 property ID: `359661193`

### Google Cloud Project:
- **Project:** Money Page Optimizer
- **Project ID:** `money-page-optimizer`
- **APIs Enabled:** Google Search Console API, Google Analytics Data API

### OAuth App:
- **App Name:** About Wall Art GSO Tools
- **Redirect URI:** `https://tools.aboutwallart.com/api/gsc`
- **Test User:** mae@aboutwallart.com
- **Scopes:** `webmasters.readonly` + `analytics.readonly` (BOTH required in same token)

### Supported Actions (GET ?action=):
- `overview` — total clicks, impressions, CTR, avg position
- `monthly` — monthly breakdown for historical view
- `queries` — all queries with impressions (rowLimit: 1000)
- `pages` — top pages by clicks (rowLimit: 500)
- `page-query` — page + query combined (rowLimit: 5000)
- `device` — device breakdown
- `blog-tracking` — performance of specific URLs (pass urls= param)
- `keyword-monthly` — monthly impressions+clicks for exact keyword (pass keyword= param) — used by Keyword Tracker growth chart
- `ga4-traffic-breakdown` — GA4 sessions by page path and month (rowLimit: 5000) — used by Suspicious Traffic stacked chart
- `ga4-suspicious` — GA4 sessions with bounce rate by month — used by Suspicious Traffic gauge
- `ga4-llm` — GA4 LLM/AI referral traffic (?days=30/90/180)

### CRITICAL: URL Format
GSC returns URLs WITHOUT www: `https://aboutwallart.com/`
Shopify builds URLs WITH www: `https://www.aboutwallart.com/`
Always normalise before matching:
```javascript
url.replace('https://www.aboutwallart.com', 'https://aboutwallart.com')
```

### How to get a new Refresh Token:
IMPORTANT: Must include BOTH scopes in one token.
Use this URL:
```
https://accounts.google.com/o/oauth2/v2/auth?client_id=15466013888-deual871humreqd3uqhrve8chp3bci3s.apps.googleusercontent.com&redirect_uri=https://tools.aboutwallart.com/api/gsc&response_type=code&scope=https://www.googleapis.com/auth/webmasters.readonly%20https://www.googleapis.com/auth/analytics.readonly&access_type=offline&prompt=consent
```
1. Visit URL, sign in as mae@aboutwallart.com, click Allow
2. Copy `code` from redirect URL IMMEDIATELY (expires in ~60 seconds)
3. Go to hoppscotch.io — POST — `https://oauth2.googleapis.com/token`
4. Body (x-www-form-urlencoded):
   - code = [code from step 2]
   - client_id = 15466013888-deual871humreqd3uqhrve8chp3bci3s.apps.googleusercontent.com
   - client_secret = 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
   - redirect_uri = https://tools.aboutwallart.com/api/gsc
   - grant_type = authorization_code
5. Click Send immediately — copy refresh_token from response
6. Update GSC_REFRESH_TOKEN in Vercel env vars
7. Redeploy Vercel

---

## blogs.js — Full Technical Details

**File:** `api/blogs.js`
**Last Updated:** May 30, 2026
**Purpose:** Blog Manager tool backend + all Easy SEO Report data storage actions

### GET Actions (?action=):

#### get-published-keywords
- Returns all Published Blog + To_Write_Blog keywords from registry CSV
- Returns: `{ success: true, publishedBlogs: [{keyword, url}] }`

#### get-registry
- Returns full keyword-locker-registry.csv as structured array
- Returns: `{ success: true, registry: [{keyword, url, locked, status, action}] }`
- Only rows with locked=LOCKED + status=DONE + action=TO_OPTIMIZE or OPTIMIZED count as truly locked

#### get-revenue-by-month
- Fetches last 13 months of revenue via Shopify REST Orders API
- Returns: `{ success: true, revenue: { YYYYMM: amount } }`
- NOTE: Currently returning 0 values — Shopify read_orders scope may need verifying
- Skips voided/refunded orders

#### get-susp-events
- Returns saved suspicious traffic chart events from data/suspicious-events.json
- Returns: `{ success: true, events: [{label, date}] }`

#### get-optimisations
- Returns keyword optimisation dates from data/keyword-optimisations.json
- Returns: `{ success: true, optimisations: { tabId: 'YYYY-MM-DD' } }`

#### get-tracked-keywords
- Returns tracked keyword list from data/tracked-keywords.json
- Returns: `{ success: true, keywords: ['kw1', 'kw2', ...] }`

#### get-keyword-tabs
- Returns keyword tracker tabs from data/keyword-tracker.json
- Returns: `{ success: true, tabs: [{id, label, keyword}] }`

#### GET (no action) — Blog ideas pipeline (for Blog Manager tool)

### POST Actions (req.body.action):

#### lock-keyword
- Saves keyword+URL to keyword-locker-registry.csv on GitHub
- Format: LOCKED, DONE, TO_OPTIMIZE, User Resolved
- Body: `{ action: 'lock-keyword', keyword, url }`

#### save-susp-events
- Saves suspicious traffic chart events to data/suspicious-events.json
- Body: `{ action: 'save-susp-events', events: [{label, date}] }`

#### save-optimisations
- Saves keyword optimisation dates to data/keyword-optimisations.json
- Body: `{ action: 'save-optimisations', optimisations: { tabId: 'YYYY-MM-DD' } }`

#### save-tracked-keywords
- Saves tracked keywords list to data/tracked-keywords.json
- Body: `{ action: 'save-tracked-keywords', keywords: [...] }`

#### save-keyword-tabs
- Saves keyword tracker tabs to data/keyword-tracker.json
- Body: `{ action: 'save-keyword-tabs', tabs: [{id, label, keyword}] }`

#### POST (no action) — Mark blog as To Write (Blog Manager)
#### PATCH — Mark as Keyword Change (Blog Manager)
#### DELETE — Undo (Blog Manager)

### Google Sheets Integration:
- Webhook URL in Vercel env var: SHEETS_WEBHOOK_URL
- Non-blocking
- Google Sheet ID: 1W9AYH2Zcw-v2U6S47a3w5bs5rEBf6K30adDgNkaZ38k

---

## /data/ Folder — Data Files

### Existing files:
- `keyword-locker-registry.csv` — Central keyword registry (378+ locked keywords)
- `blog_ideas.csv` — Blog idea pipeline with conflict detection

### New files added May 30, 2026:
- `keyword-tracker.json` — Keyword Tracker tab definitions `[{id, label, keyword}]`
- `tracked-keywords.json` — Selected keywords in Keyword Rankings `['kw1', 'kw2']`
- `keyword-optimisations.json` — Optimisation dates per keyword tab `{ tabId: 'YYYY-MM-DD' }`
- `suspicious-events.json` — Suspicious Traffic chart event markers `[{label, date}]`

---

# EASY SEO REPORT TOOL — COMPLETE DOCUMENTATION

**File:** `gsc-report.html`
**Current Version:** v52 (May 30, 2026)
**URL:** https://tools.aboutwallart.com/gsc-report.html

## Sidebar Sections (in order):
1. High Level SEO Metrics
2. Historical View (1 Year)
3. 🚨 Suspicious Traffic
4. 🤖 AI Traffic
5. 🔥 Quick Wins
6. ⚠️ Danger Pages
7. Keyword Tracker
8. Keyword Rankings
9. Blog Optimizations
10. Content Optimizations

---

## 1. High Level SEO Metrics
- Date range selector: 7/28/90/180 days
- 4 metric cards: Clicks, Impressions, CTR, Avg Position
- Line chart: daily clicks vs previous period
- Device donut chart
- Landing pages table + Queries table

## 2. Historical View (1 Year)
- Gauge charts with % change vs same month previous year
- Monthly Clicks bar chart with Net Sales overlay (dual Y axis — impressions left, revenue right)
- Monthly Impressions bar chart
- NOTE: Net Sales currently showing 0 — Shopify revenue API needs debugging

## 3. 🚨 Suspicious Traffic
- GA4 bounce rate data as proxy for suspicious/bot traffic
- Gauge: current month suspicious % vs same month last year
- Stacked bar chart: two bars per month
  - Left bar: total sessions (violet)
  - Right bar: stacked breakdown — 🔴 Suspicious | 🟣 Collections | 🟢 Blogs | 🟠 Products | 🔵 Pages | 🟡 Ad Intent
- Ad Intent URL tracked: `/collections/art-prints-for-wall`
- 📌 Chart Events panel: add named events with dates (e.g. "Ad Intent budget added")
  - Saved to data/suspicious-events.json on GitHub
  - Shows as vertical coloured dashed lines on chart with labels
- Summary table: monthly breakdown with risk levels (Low/Medium/High)

## 4. 🤖 AI Traffic
- GA4 Data API via ga4-llm action
- Per-source cards (ChatGPT, Perplexity, Claude, Gemini, Copilot, etc.)
- Each card: gauge (outer=views, inner=sessions) + bar chart (6 months fixed)
- Top overview: donut chart (sessions by source) + 12-month line chart
- Date range: 30/90/180 days selector
- Duplicate sources merged (chatgpt.com + chat.openai.com = one card)

## 5. 🔥 Quick Wins
- Pages ranking position 1–20 with 100+ impressions
- Page type sub-tabs: Collections | Blogs | Products | Pages
- All / Selected sub-tabs with + Select and ↩ Undo buttons
- Column sort: click any header for priority-based sorting (1↑ badges)
- Registry column: 🔒 Locked | 🟡 Keyword taken | 🔴 URL taken
- Save to Registry: locks keyword+URL with LOCKED/DONE/TO_OPTIMIZE/User Resolved
- Search bar + data loads independently

## 6. ⚠️ Danger Pages
- Pages where clicks dropped 20%+ vs previous 90 days (min 10 clicks previous period)
- Same structure as Quick Wins: page type sub-tabs, All/Selected, sort, registry
- Keyword column showing top ranking keyword per URL
- Save to Registry: same rules as Quick Wins
- Loads independently (does not depend on Quick Wins loading first)

## 7. Keyword Tracker
- Named tabs saved to GitHub (keyword-tracker.json)
- Each tab tracks a keyword and shows:
  - 5 metric cards (Queries Found, Clicks, Impressions, CTR, Avg Position)
  - 📅 Optimisation date marker: date input, Save Date button, green badge, Clear button
  - Growth chart (12 months): Impressions + Clicks on single Y axis
  - Vertical green dashed line on chart at optimisation month
  - Query table: all queries containing the keyword with Ranking URL column (clickable)
- Tabs are renameable and deletable
- Optimisation dates saved to data/keyword-optimisations.json

## 8. Keyword Rankings
- All GSC queries with metrics for last 90 days
- Filters: Search, Position range, Difficulty, Min Impressions, Row limit
- Column sort: click any header, priority badge system (1↑, 2↓ etc), ✕ to remove
- Difficulty: Easy/Medium/Hard calculated from position + impressions + CTR
- All tab: + Select button moves keyword to Selected tab
- Selected tab: Ranking URL, Registry (🔒/🟡/🔴), ↩ Undo, Save to Registry
- Trend column in Selected tab: ▲ green / ▼ red / ▶ amber vs previous 90 days
- Saved selections in data/tracked-keywords.json

## 9. Blog Optimizations
- Blog posts only (/blogs/ URLs)
- Excludes: CDN, /es/, /fr/, ?param URLs, Quick Wins URLs, Danger Pages URLs
- 3 tabs: 🔴 Urgent (CTR <0.5%) | 🟡 Improve (0.5%-1%) | ⚪ Monitor (>1%)
- Sorted by lowest clicks first within each tab
- Filters: 100+ impressions, published >90 days ago, English only
- Download CSV per tab

## 10. Content Optimizations
- Non-blog pages only
- Excludes: CDN, /es/, /fr/, ?param URLs, Quick Wins URLs, Danger Pages URLs
- Fix CTR: position 1-7, has clicks, CTR <2%, 100+ impressions
- Pages to Optimise: position 8-15, CTR <2%, 100+ impressions
- Low Ranking Opportunities: queries with no matching registry content, 10+ impressions
- All tables: search, row count, Download CSV

## URL Cleaning Rules (applied everywhere):
```javascript
function cleanUrl(rawUrl) {
  // Strip domain, strip ?query params, strip trailing slash
}
function shouldExcludeUrl(url) {
  // Exclude: /cdn/, /es/, /fr/, /es, /fr, ?_pos=, ?_sid=, .js, .css
}
```

## API Calls Made by gsc-report.html:
- /api/gsc?action=overview
- /api/gsc?action=monthly
- /api/gsc?action=pages
- /api/gsc?action=queries
- /api/gsc?action=page-query
- /api/gsc?action=device
- /api/gsc?action=ga4-llm
- /api/gsc?action=ga4-suspicious
- /api/gsc?action=ga4-traffic-breakdown
- /api/gsc?action=keyword-monthly&keyword=X
- /api/shopify-files?action=get-blogs-by-month&month=YYYY-MM
- /api/blogs?action=get-published-keywords
- /api/blogs?action=get-registry
- /api/blogs?action=get-keyword-tabs
- /api/blogs?action=get-tracked-keywords
- /api/blogs?action=get-optimisations
- /api/blogs?action=get-susp-events
- /api/blogs?action=get-revenue-by-month

---

# API KEYS & CREDENTIALS

## Vercel Environment Variables (May 30, 2026)

```
ANTHROPIC_API_KEY         — Claude API key (Production only)
OPENAI_API_KEY            — OpenAI API key (unused)
SHOPIFY_STORE_DOMAIN      — aboutwallart.myshopify.com
SHOPIFY_ACCESS_TOKEN      — Shopify Admin API token (read_analytics scope added May 30)
TOOLS_PASSWORD            — Password for tools dashboard
GOOGLE_API_KEY            — Google API key (PageSpeed)
SERPAPI_KEY               — SerpAPI key (Production only)
BLOGDATA_READ_WRITE_TOKEN — Vercel Blob storage token
EDGE_CONFIG               — Edge config (all environments)
GITHUB_TOKEN              — GitHub API token for CSV/JSON files
SHEETS_WEBHOOK_URL        — Google Apps Script webhook URL
GSC_CLIENT_ID             — 15466013888-deual871humreqd3uqhrve8chp3bci3s.apps.googleusercontent.com
GSC_CLIENT_SECRET         — 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
GSC_REFRESH_TOKEN         — Updated May 29, 2026 (includes GSC + GA4 scopes)
GA4_PROPERTY_ID           — 359661193
```

---

# DEPLOYMENT

## GitHub to Vercel Workflow
1. Upload/edit file on GitHub (main branch)
2. Vercel auto-deploys (2-3 minutes)
3. Wait 2 minutes before testing
4. Hard refresh: Cmd+Shift+R (Mac) or Ctrl+Shift+R (Windows)

## Debugging
- Vercel logs: https://vercel.com/aboutwallarts-projects/seo-tools/logs

---

# DEPLOYED TOOLS

**Primary Domain:** https://tools.aboutwallart.com (Password Protected)

1. Keyword Locker — /keyword-locker.html
2. Blog Manager — /blog-manager-tool.html
3. Money Page Doctor — /money-page-doctor.html
4. Rank Report Card — /rank-report-card.html
5. Image Optimizer — /image-optimizer.html
6. Easy SEO Report — /gsc-report.html (LIVE v52)

---

# KNOWN ISSUES & TODO

## Revenue Chart (Historical View)
- Net Sales bars showing £0 — Shopify Orders REST API returning empty
- Tried ShopifyQL (requires read_analytics scope — Basic plan limitation)
- Tried REST Orders API — returning empty
- TODO: Debug with Shopify support or regenerate access token with read_orders scope confirmed

## Shopify Access Token
- read_analytics scope was added May 30, 2026 but token was NOT regenerated
- ShopifyQL requires token reinstall to pick up new scopes
- TODO: Reinstall app in Shopify admin to regenerate token, update SHOPIFY_ACCESS_TOKEN in Vercel

---

# TROUBLESHOOTING

## GSC API fails
- Check GSC_CLIENT_ID, GSC_CLIENT_SECRET, GSC_REFRESH_TOKEN in Vercel
- Refresh token must include BOTH scopes
- Follow "How to get a new Refresh Token" steps above

## GA4 data fails
- Check GA4_PROPERTY_ID = 359661193 in Vercel
- Check GSC_REFRESH_TOKEN includes analytics.readonly scope
- GA4 Data API must be enabled in money-page-optimizer Google Cloud project

## Revenue showing £0
- See Known Issues above
- Check Vercel logs for /api/blogs?action=get-revenue-by-month errors

## Quick Wins / Danger Pages showing 0
- These load independently — check browser console for errors
- Registry cross-reference requires kwrInit() to complete first

## Blog Optimizations slow
- Fetches 16 months of Shopify data — expected, takes a few seconds

## CORS Errors
- Verify CORS headers in API file
- Verify vercel.json CORS config
- Redeploy after changes

## Vercel function limit
- 11/12 (reddit.js and openai.js unused)
- Max 1 more API file before removing unused ones

---

# CONTACT & SUPPORT

**GitHub:** https://github.com/aboutwallart/seo-tools
**Vercel:** https://vercel.com/aboutwallart/seo-tools
**Live Tools:** https://tools.aboutwallart.com
**Shopify Admin:** https://aboutwallart.myshopify.com/admin
**Google Sheet:** https://docs.google.com/spreadsheets/d/1W9AYH2Zcw-v2U6S47a3w5bs5rEBf6K30adDgNkaZ38k
**Google Cloud:** https://console.cloud.google.com (project: money-page-optimizer)
**GA4:** https://analytics.google.com (property ID: 359661193)

---

Document End

Update this document when:
- New tools or actions added
- API keys or env vars changed
- Bugs fixed
- Features added
- Vercel function count changes
- New data files added to /data/ folder

---

# LINK WHISPERER TOOL — COMPLETE DOCUMENTATION

**File:** `link-whisperer.html`
**Current Version:** v16.6 (June 1, 2026)
**URL:** https://tools.aboutwallart.com/link-whisperer.html
**Purpose:** Internal linking manager — replicates Link Whisper (WordPress plugin) for Shopify. Manages inbound and outbound internal links across all site content.

---

## API Backend

All Shopify calls go through `api/shopify-files.js` using two new action branches added to the existing file. No new API files were created (Vercel function count remains 11/12).

### New action branches in `api/shopify-files.js`:

#### `action=link-whisperer` (REST proxy — GET and PUT)
- Reads `endpoint` and `method` query params
- Forwards to: `https://${SHOPIFY_STORE_DOMAIN}/admin/api/2025-01/${endpoint}`
- Header: `X-Shopify-Access-Token`
- Passes through Shopify `Link` header for pagination
- Used for: reading products, pages, articles, collections, writing body HTML changes

#### `action=link-manager` (REST proxy — identical logic, separate action name)
- Same as link-whisperer but triggered by `action=link-manager`
- Originally planned as a separate tool — kept for backwards compatibility

### Shopify API Endpoints Used:
- `products.json?limit=250&fields=id,title,handle,published_at,body_html,product_type,tags,vendor`
- `pages.json?limit=250&fields=id,title,handle,published_at,body_html`
- `blogs.json?limit=250&fields=id,title,handle`
- `blogs/{blog_id}/articles.json?limit=250&fields=id,title,handle,published_at,body_html,blog_id,tags`
- `custom_collections.json?limit=250&fields=id,title,handle,published_at,body_html`
- `smart_collections.json?limit=250&fields=id,title,handle,published_at,body_html`
- `custom_collections.json?product_id={id}&fields=id,title,handle&limit=250` — gets collections a product belongs to
- `smart_collections.json?product_id={id}&fields=id,title,handle&limit=250` — gets smart collections a product belongs to
- `products/{id}.json` PUT — updates product body_html
- `pages/{id}.json` PUT — updates page body_html
- `blogs/{blog_id}/articles/{id}.json` PUT — updates article body_html
- `custom_collections/{id}.json` PUT — updates collection body_html
- `smart_collections/{id}.json` PUT — updates smart collection body_html

### Required Shopify API Scopes:
- `read_products` — read products, collections, collects
- `write_products` — write product body_html
- `read_content` — read pages and blog articles
- `write_content` — write pages and blog articles body_html

---

## localStorage Cache System

The tool caches all Shopify data in the browser to avoid re-syncing on every visit.

| Key | Contents | Expiry |
|-----|----------|--------|
| `lw-cache` | Slim items array (no bodyHtml) + links array + lastSync timestamp | 24 hours |
| `lw-body-cache` | Map of itemId → bodyHtml for all items | 24 hours |
| `lw-cache-time` | Unix timestamp of last sync | Used to calculate age |

Cache is invalidated when:
- User clicks "Refresh Data" button
- 24 hours have passed since last sync

---

## Tool Architecture

### AppState (global state object):
```javascript
const AppState = {
  items: [],          // all items: {id, shopifyId, title, handle, type, publishedAt, bodyHtml, tags, vendor, url, blogId?, blogHandle?, collectionIds?}
  links: [],          // all parsed links: {sourceId, sourceUrl, targetUrl, anchorText, type}
  brokenLinks: [],    // {sourceId, sourceUrl, brokenUrl, anchorText, type, status, discovered}
  lastSync: null,
  linkGraph: {
    outbound: {},     // {itemId: [{targetUrl, anchorText, type}]}
    inbound: {}       // {itemId: [{sourceId, sourceUrl, anchorText}]}
  },
  lrPage: 1,
  lrSort: [],         // array of {col, dir} for multi-column sort
  expandState: {},
  suggestState: {},
  inboundSuggestState: {},
  saveStatuses: {},   // {itemId: {status: 'saving'|'saved'|'failed'|'queued', count: N}}
  linkQueue: []       // [{isInbound, item/targetItem, phrases/selections, label}]
};

const BlState = { page:1, selected: new Set(), sort:{ col:'', dir:'' } };
```

### Item Types:
- `product` — Shopify products
- `page` — Shopify pages
- `article` — Shopify blog articles
- `collection` — Custom and smart collections (both stored as type `collection`)

### Link Type Detection:
- `internal` — href contains `aboutwallart.com` or starts with `/`
- `external` — all other hrefs

---

## Page Type Rules (for link suggestions)

### getPageLinkType(item) returns:
- `product` — item.type === 'product'
- `collection` — item.type === 'collection'
- `blog` — item.type === 'article'
- `trend` — item.type === 'page' AND title matches TREND_PAGES list
- `static` — item.type === 'page' AND not a trend page

### TREND_PAGES (full list):
Biophilic Interior Design Trend, Zen Room Decorations Trend, Tropical Decor For Home Style Trend, Transitional Interior Design Trend, Preppy Style Interiors Decor Trend, Old Money Home Decor Trend, Coffee House Interior Design Trend, Masculine Living Room Ideas, Black & White Home Decor Trend, Wildlife Home Decor Trend, Scandi Home Decor Trend, Country Cottage Home Decor Trend, Morroccan Home Decor Trend, Cosy Minimalism Home Decor Trend, Mediterranean Home Decor Trend, Japandi Home Decor Trend, Industrial Home Decor Trend, French Country Home Decor Trends, Modern Farmhouse Home Decor Trend, Eclectic Maximalist Home Decor Trend, Modern Contemporary Home Decor Trend, Coastal Home Decor Trend, Boho Home Decor Trend, Modern Glam Luxe Interiors Decor Trend, Mid Century Home Decor Trend

### PAGES_TO_NEVER_TOUCH (no + Add button shown):
Free Interior Design Education & Guides, Free Home Decor & Interior Design Tools, Home Decor by Trend, Home Decor by Room, Home Decor by Product, all calculator tools, all guide pages, 404, Wishlist, Loyalty Page, Confirm Your Email, Keep in Touch, Spread the Love, Birthday page, Free Workbook, Product Bundles, Unsure About the Size, Write for Us, Homepage

---

## Outbound Link Rules (pre-written phrases)

### Product pages — needs: 1 collection + 1 blog
- Collection phrase: `"This product is part of our [Collection] collection — explore in full to discover more pieces that complement this style perfectly."`
- Blog phrase: `"Looking for inspiration? Our article [Blog Title] is full of ideas to help you style this piece in your home."`
- Added at: end of product description

### Collection pages — needs: 1 blog
- Blog phrase: `"Not sure where to start? Our guide [Blog Title] is packed with ideas to help you style these pieces in your home."`
- Added at: end of collection description

### Trend pages — needs: 1 collection + 1 blog
- Collection phrase: `"Explore our [Collection] collection to shop this trend."`
- Blog phrase: `"You may also want to read our article [Blog Title] for more styling ideas."`
- Added at: end of trend page body

### Blog posts — needs: 1 collection
- Collection phrase: `"Browse our [Collection] collection to find pieces that bring this look to life in your home."`
- Added at: end of blog post body

### Static pages — needs: 1 collection + Content Hub link
- Phrase: `"Browse our [Collection] collection or visit our Content Hub for styling inspiration and decorating guides."`
- Content Hub URL: `https://aboutwallart.com/pages/free-interior-design-education-guides`
- Added at: end of page body

---

## Inbound Link Rules (pre-written phrases)

### Trend pages — needs: 2 blog inbound + 1 trend page inbound
- Blog → Trend phrase: `"If you love this style, explore our full guide to [Trend Page Title] for more ideas and inspiration."`
- Trend → Trend phrase: `"If you enjoy this style, you may also love our guide to [Trend Page Title]."`

### Collection pages — needs: 1 trend inbound + 1 blog inbound
- Trend → Collection phrase: `"Explore our [Collection Title] collection to find the perfect items for this trend."`
- Blog → Collection phrase: `"Browse our [Collection Title] collection to find pieces that will bring this look to life in your home."`

### Blog posts — needs: 1 blog inbound + 1 trend inbound
- Blog → Blog phrase: `"You may also enjoy our article [Blog Post Title] for more styling ideas."`
- Trend → Blog phrase: `"You may also want to read our article [Blog Post Title] for more styling ideas."`

---

## Collection Membership Detection

For product pages, the tool fetches which collections the product actually belongs to on-demand when + Add is clicked:

```javascript
// Two calls run in parallel:
GET custom_collections.json?product_id={shopifyId}&fields=id,title,handle&limit=250
GET smart_collections.json?product_id={shopifyId}&fields=id,title,handle&limit=250
// Results merged — all collection IDs stored on item.collectionIds
```

This is fetched once per product per session and cached on the item object.

---

## Queue System

Items can be added to a queue instead of saving immediately:
- "Add to Queue" button in the suggestion panel — no preview modal, instant queue
- "Review & Save Now" — shows approval modal with phrase preview before saving
- Queue bar appears at top of page showing count
- "Save Queue (N)" button saves all queued items sequentially with progress counter
- After save: dedicated result modal shows how many saved + any failures by item name
- "Clear Queue" removes all items without saving

---

## Bulk Systems

### Existing queue system: unchanged — individual + Add buttons per row

### Keyword Based Bulk Mode (new — purple button in toolbar):
- Search bar + item type + vendor filters
- Type keyword, press Enter or click SEARCH
- Shows all matching items that still need links added
- Each item has a checkbox and expands to show the suggest panel
- Blog options filtered to only show blogs containing the same keyword
- Blog with fewest inbound links is pre-selected
- "Add to Queue" per item, or "Add All Selected to Queue" for bulk queue

---

## Tabs Built and Working

| Tab | Status |
|-----|--------|
| Dashboard | ✅ Working — stats cards, top domains, internal vs external donut chart |
| Links Report | ✅ Working — full table, filters, multi-column sort, vendor filter, + Add, inbound/outbound, queue |
| Domains Report | ✅ Working — table with expand rows, sort, filter, CSV export |
| Broken Links Report | ✅ Working — search bar fix per row, Remove Link, column sorting |
| Auto Linking | ⏳ Not yet built |
| URL Changer | ⏳ Not yet built |
| Target Keywords | ⏳ Not yet built |
| Settings | ⏳ Not yet built |

---

## Known Issues / Still To Fix (as of v16.6, June 1, 2026)

1. **Outbound column sort** — sorts by number of links, not by ✓ Done / + Add grouping
2. **Keyword Bulk Mode — speed** — 48 articles takes too long to process
3. **Keyword Bulk Mode — auto-expand** — selected items should auto-expand without clicking one by one
4. **Keyword Bulk Mode — multiple keywords** — need to support up to 4-5 keywords separated by commas, show items matching ANY keyword
5. **Keyword Bulk Mode — saved items** — after queue save, items should update to show ✓ Done status
6. **Inbound Bulk Mode** — Keyword Based Bulk Mode not yet added to Inbound Internal Links section
7. **Resolved tab** — not yet built — should show last 50 fixed broken links with undo option
8. **Broken Links bulk save** — not yet built
9. **Cache** — still syncing twice on some sessions instead of loading from cache

---

## Vercel Function Count (Updated June 1, 2026)
- **Current count: 11/12** — no new files created for Link Whisperer
- All new logic added as action branches inside existing `api/shopify-files.js`
- 1 slot still available

